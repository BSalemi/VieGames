<h1><%= @game.title %> </h1>

<% rating = @game.content_rating.downcase.gsub(" ","_") %>
<h6> Content Rating:</h6> <%= link_to @game.content_rating, rating_path(rating)%> </br>
<h6> Game System: </h6> <%= link_to @game.system.name, system_path(@game.system) %></br>
<h6> Company:</h6> <%= link_to @game.company.name, company_path(@game.company)%></br>
<h6> Number of Users who play <%= @game.title %>:</h6> <%= @game.users.count %><br>
<% if @game.user_games.count > 0 %>
<h6> Average User Rating:</h6> <%= @game.average_rating %>
<% end %> 


<% user_game = UserGame.find_by(game_id: @game.id, user_id: current_user.id) %>
<% if current_user.games.find{|game| game.id == @game.id} && user_game.star_rating == nil %>
    <%= form_for(:user_game, :url => add_rating_path, method: "patch") do |f| %>
        <h6> Add a Rating for <%= @game.title %> </h6>
        1 <%= f.radio_button(:star_rating, 1)%>
        2 <%= f.radio_button(:star_rating, 2)%>
        3 <%= f.radio_button(:star_rating, 3) %>
        4 <%= f.radio_button(:star_rating, 4) %>
        5 <%= f.radio_button(:star_rating, 5) %></br>
        <%= f.submit "Add Rating" %>
    <% end %>
<% end %></br>

<% if current_user.games.find{|game| game.id == @game.id} %>
    <%= link_to "Remove #{@game.title} from Your Collection", delete_game_path(@game) %><br>
<% else %>   
    <%= link_to "Add #{@game.title} to Your Collection", add_game_path(@game) %> <br>
<% end %> <br> 

<%= link_to "Upcoming Events for #{@game.title}", game_events_path(@game)%> <br><br>

<% if current_user.games.find{|game| game.id == @game.id} %>
    <%= link_to "Create an Event for #{@game.title}", new_game_event_path(@game)%> <br>
<% end %><br>
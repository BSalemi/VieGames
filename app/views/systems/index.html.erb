<h1> Video Game Systems </h1>

<% @systems.each do |system| %>
    <h3><li><%= link_to system.name, system_path(system)%> </li></h3>
<% end %>

<%= link_to "Return Home", user_path(session[:user_id]) %>